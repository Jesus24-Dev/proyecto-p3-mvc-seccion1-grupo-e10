# Variables para docker-compose (copia este archivo a .env en la raíz).

# Secreto para firmar los JWT. Genera uno con: openssl rand -hex 32
JWT_SECRET=

# Puertos del host (opcionales).
# BACKEND_PORT=3001
# DB_PORT=5432
